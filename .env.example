# Copiar este archivo como .env y rellenar con los datos reales.
# El .env NO se sube a git y el .htaccess lo bloquea desde la web.
# install.php lo genera automáticamente.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=nombre_base_de_datos
DB_USER=usuario_bd
DB_PASS=contraseña_bd
